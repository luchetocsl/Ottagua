---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout>
	<div class="text-red flex flex-col items-start justify-start gap-8 max-w-[1240px] mx-auto px-4 py-8 w-full">
		<div class="w-full">
			<h2 class="text-2xl font-semibold text-red text-center mb-8 max-w-md mx-auto">Ayúdame a encontrar mejores noticias</h2>
		</div>

		<div class="flex flex-row items-start justify-center gap-8 w-full md:flex-row">
			<div class="max-w-md w-full">
				<p class="mb-4 text-red leading-7">
					Estoy siguiendo varios grupos latinos en Facebook, sigo a
					todas las embajadas que he podido encontrar, y sigo las
					noticias locales para curar las noticias más importantes
					para nuestra comunidad. Pero siempre estoy descubriendo
					nuevas fuentes.
				</p>

				<p class="mb-4 text-red leading-7">
					Conoces alguna fuente que debería checar? Por favor,
					compártela conmigo.
				</p>
			</div>
			<div class="self-center max-w-xl w-full">
				<iframe
					data-tally-src='https://tally.so/embed/3yYzD4?alignLeft=1&hideTitle=1&transparentBackground=1&dynamicHeight=1'
					loading='lazy'
					width='100%'
					height='600'
					frameborder='0'
					marginheight='0'
					marginwidth='0'
					title='Ayuda a Ottagua'></iframe>
			</div>
		</div>
	</div>
</BaseLayout>

<!-- All styling now handled by Tailwind classes -->
<style>
	@media screen and (max-width: 600px) {
		.flex-row {
			@apply flex-col;
		}
		.gap-8 {
			@apply gap-0;
		}
		.mb-8 {
			@apply mb-4;
		}
	}
</style>

<script>
	// @ts-ignore - Tally is loaded from external script
	var d = document,
		w = "https://tally.so/widgets/embed.js",
		v = function () {
			// @ts-ignore - Tally is loaded from external script
			"undefined" != typeof Tally
				? Tally.loadEmbeds()
				: d
						.querySelectorAll("iframe[data-tally-src]:not([src])")
						.forEach(function (e) {
							if (e instanceof HTMLIFrameElement) {
								// @ts-ignore - dataset property exists on HTMLIFrameElement
								e.src = e.dataset.tallySrc;
							}
						});
		};
	// @ts-ignore - Tally is loaded from external script
	if ("undefined" != typeof Tally) v();
	else if (d.querySelector('script[src="' + w + '"]') == null) {
		var s = d.createElement("script");
		(s.src = w), (s.onload = v), (s.onerror = v), d.body.appendChild(s);
	}
</script>
