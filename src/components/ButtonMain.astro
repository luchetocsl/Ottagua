---
const { title, link } = Astro.props;
import "remixicon/fonts/remixicon.css";
---

<a href={link} class:list={["button"]}
	>{title} <i class='ri-arrow-right-up-line'></i></a
>

<style>
	.button {
		background-color: var(--red);
		padding: 1rem 2rem;
		border-radius: 2rem;
		text-decoration: none;
		color: var(--cream);
		margin: 1rem 0;
		display: inline-block;
		font-size: 1rem;
		width: fit-content;
	}
</style>
